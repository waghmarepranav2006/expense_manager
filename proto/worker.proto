syntax = "proto3";

package lb_demo;

service WorkerService {
  rpc HandleRequest(WorkRequest) returns (WorkReply);
}

message WorkRequest {
  int32 request_id = 1;
}

message WorkReply {
  int32 request_id = 1;
  string handled_by = 2;
  string status = 3;
}